package com.nmmoc7.a2kn1gh7.magic.circle;

import net.minecraft.util.math.vector.Vector3d;

/**
 * @author DustW
 */
public class ChildCircle extends CoreCircle{
    public ChildCircle(String regName, Vector3d axialVector, Vector3d centerRelativePosition, double rotationSpeed, double size, int forward, int life, int color) {
        super(regName, axialVector, centerRelativePosition, rotationSpeed, size, forward, life, color);
    }
}
